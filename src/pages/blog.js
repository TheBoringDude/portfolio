import React from 'react';

import Layout from '../components/layout';
import PageHeader from '../components/pageheader';

const BlogPage = () => (
  <Layout title="Blog">
    <PageHeader title="My Personal Blog" info="I write too, its just that I cannot focus myself into it..." />
  </Layout>
);

export default BlogPage;
